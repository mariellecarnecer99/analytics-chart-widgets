{
  "name": "analytics-chart-widgets-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dashboard": "npm --prefix apps/dashboard run dev",
    "container": "npm --prefix apps/chart-container run dev",
    "dev": "npm-run-all --parallel dashboard container",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
